# API Configuration
# The base URL for your backend API
NEXT_PUBLIC_API_URL=https://backend.juicebar.hasanalicollege.com/api/v1

# Cache Revalidation Secret
# This secret is used to revalidate cached data (must match backend)
REVALIDATE_SECRET=your-secret-key-here
